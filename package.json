{
  "name": "trello",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server -p 7000 -w db.json\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server -p 7000 -w db.json"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "sass": "^1.69.4"
  }
}
